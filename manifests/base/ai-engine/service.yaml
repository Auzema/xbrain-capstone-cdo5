apiVersion: v1
kind: Service
metadata:
  name: ai-engine
  labels:
    app.kubernetes.io/name: ai-engine
spec:
  selector:
    app: ai-engine
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
  type: ClusterIP
