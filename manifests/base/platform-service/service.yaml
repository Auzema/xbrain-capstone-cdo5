apiVersion: v1
kind: Service
metadata:
  name: platform-service
  namespace: aiops
  labels:
    app: platform-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: platform-service
