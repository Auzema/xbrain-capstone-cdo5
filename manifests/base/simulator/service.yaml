apiVersion: v1
kind: Service
metadata:
  name: simulator
  labels:
    app: simulator
  annotations:
    team: platform
spec:
  selector:
    app: simulator
  ports:
    - name: metrics
      protocol: TCP
      port: 80
      targetPort: 8080
